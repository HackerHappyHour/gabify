# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.define "docker-host" do |d|
    d.vm.box = "centos/atomic-host"

    d.vm.provider "virtualbox" do |vb|
      vb.linked_clone = "true"
    end

  end

end
